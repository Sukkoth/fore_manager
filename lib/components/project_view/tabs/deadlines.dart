import 'package:flutter/material.dart';

class ProjectDeadlines extends StatelessWidget {
  const ProjectDeadlines({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Project Deadlines",
        style: TextStyle(fontSize: 20),
      ),
    );
  }
}
